*
  box-sizing border-box
.slideIn-enter-active, .slideIn-leave-active, .slideOut-enter-active, .slideOut-leave-active
  will-change transform
  transition all 0.5s
  position absolute
  width 100vw
  height 100vh
  overflow hidden
  z-index 1
.slideOut-leave-active
  z-index 2
.slideIn-enter-from
  opacity 0
  transform translate3d(100%, 0, 0)
.slideIn-leave-to
  transform translate3d(0, 0, 0)
.slideOut-enter-from
  transform translate3d(0, 0, 0)
.slideOut-leave-to
  opacity 0
  transform translate3d(100%, 0, 0)